<%= form_for(@city) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <p><label for="city_state">State</label><br/>
    <%= collection_select :city, :state_id, @states, :id, :name%>
    
  <div class="field">
 
    <%= label :city, :name %><br />
  <%= text_field :city, :name%>
 </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<!--script type="text/javascript">
  //<![CDATA[
  function check_selected_value(el) {
    if (el.value == "--States--") {
      $('name').disable
    }
    else {
      $('name').enable
    }
  }
  //]]>
  </script-->

