<h1>Listing skillsets</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @skillsets.each do |skillset| %>
  <tr>
    <td><%=skillset.name %></td>
    <td><%= link_to 'Edit', edit_skillset_path(skillset) %></td>
    <td><%= link_to 'Destroy', skillset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Skillset', new_skillset_path %>
