<h3>Welcome <%= @candidate.name %></h3>
<h3>Search for Jobs</h3>


<%= form_tag :action => 'search', :method => 'get' do %>
  <table>
    <tr>
     <td><%= label_tag :title%></td>
     <td><%= text_field_tag :title%></td>
     <br>
   

    <td><label for="position_experience">Experience:</label></td>
	<td><%= select_tag :experience,options_for_select(["",">1","1-3","4-6","7-10"])%></p></td>
     <!--%= text_field_tag 'q1[]'%-->
 
   <td><label for='position[skillset_ids][]'>Skillsets:</label></td>
   <td> <%= select_tag :skillset_names,options_from_collection_for_select(@skillsets, :id, :name,Skillset.all.collect{|skillset| skillset.id})%></td>
   <!--td><%= label_tag :location%></td>
     <td><%= select_tag :location,options_from_collection_for_select(@cities, :id, :name,City.all.collect{|city| city.id})%></td-->
</tr>
</table>
    <%= submit_tag "Search"%>
 
<% end %>

 <!--  <%= label_tag 'skillset'%>
    <%= text_field_tag :search2, params[:search2] %>
    <%= submit_tag "Search"%> -->
 
<% if @search%>
  
	
   
   <% @search.each_hit_with_result do |hit, post| %>
	</dt><%= link_to post.title,"/positions/"+post.id.to_s+"/show"%></dt>
     <table border=1>
       <tr>
       <td>Company:</td>
       <td><%= link_to post.company.name,"/companies/"+post.company.id.to_s+"/show"%></td>
       </tr>
       <tr>
           <td>Location:</td>
            <td><%= Contactinfo.find(post.company.contactinfos_id).city%></td>
      </tr>
      <tr>
             <td>Experience:</td>
            <td><%= post.experience%></td>
     </tr>
      <tr>
          <td>Skillsets:</td>
         <td><%= post.skillsets.collect{|skillset| skillset.name }.join(', ')%></td>
 
      </tr>

    </table>
<% end %>


</dl>
<% end %>




