<h1>Listing email_settings</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @email_settings.each do |email_setting| %>
  <tr>
  	<td><%= email_setting.email %></td>
    <td><%= link_to 'Show', [@company, email_setting]  %></td>
    <td><%= link_to 'Edit', edit_company_email_setting_path(@company, email_setting) %></td>
    <td><%= link_to 'Destroy', [@company, email_setting], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Email setting', new_company_email_setting_path %>
