<%= form_for([:company, @email_setting]) do |f| %>
  <% if @email_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_setting.errors.count, "error") %> prohibited this email_setting from being saved:</h2>

      <ul>
      <% @email_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
